import { Text } from "react-native";
import { Container, Counter, Description } from "./syltes";

export function Card() {
  return (
    <Container>
      <Counter>90,86%</Counter>
      <Description>das refeições dentro da dieta</Description>
    </Container>
  )
}